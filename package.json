{
  "name": "feedbackly",
  "version": "0.1.0",
  "description": "A tiny feedback-widget SaaS prototype (zero dependencies).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
